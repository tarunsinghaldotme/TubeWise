# TubeWise — Environment Configuration

# ══════════════════════════════════════════════════════
# AWS Configuration
# ══════════════════════════════════════════════════════
AWS_REGION=us-east-1

# ── Authentication (use ONE of the two methods below) ──

# METHOD 1: Bearer Token (from Bedrock Console — RECOMMENDED)
# Generated from: AWS Console → Bedrock → API Keys
# Just paste the token value below and you're done.
AWS_BEARER_TOKEN_BEDROCK=your-bearer-token-here

# METHOD 2: Standard IAM Access Keys (from IAM Console)
# Only needed if you're NOT using the Bearer Token above.
# If AWS_BEARER_TOKEN_BEDROCK is set, these are ignored.
# AWS_ACCESS_KEY_ID=your-access-key-here
# AWS_SECRET_ACCESS_KEY=your-secret-key-here

# Bedrock Model Configuration
BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-4-20250514

# Notion Configuration
NOTION_TOKEN=secret_your-notion-integration-token
NOTION_PARENT_PAGE_ID=your-notion-page-id-32-hex-chars

# Optional: Default transcript language
TRANSCRIPT_LANGUAGE=en

# ══════════════════════════════════════════════════════
# Processing Thresholds (usually no need to change)
# ══════════════════════════════════════════════════════

# Transcripts under this word count use single-shot summarization.
# Above this, map-reduce strategy splits the transcript into chunks.
# WORD_THRESHOLD_SINGLE_SHOT=40000

# Transcripts above this word count get a multi-page Notion layout
# (parent page + sub-pages). Below this, everything fits on one page.
# SUB_PAGE_WORD_THRESHOLD=2500

# Notion API limit for blocks per request (usually 100).
# NOTION_BLOCK_BATCH_SIZE=100

# Seconds to wait for Claude's response. Increase for very long transcripts.
# BEDROCK_READ_TIMEOUT=300

# Maximum characters for local summary filenames.
# FILENAME_MAX_LENGTH=80

# ══════════════════════════════════════════════════════
# Async Queue Settings
# ══════════════════════════════════════════════════════

# Path to the SQLite database for the async job queue.
# QUEUE_DB_PATH=~/.tubewise/queue.db

# Default number of parallel workers.
# DEFAULT_WORKER_COUNT=2

# Log file path for worker output and debug logs.
# LOG_FILE_PATH=~/.tubewise/tubewise.log

# Console log level: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO
